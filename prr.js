function checkStorage(available, ordered) {
    let message;
    // Write your code under this line
      let message = $ordered > $available?'Not enough goods in stock!':'The order is placed, the manager will contact you';
     // Write your code above this line
    return message;
  }